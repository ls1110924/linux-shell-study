#!/bin/zsh
script_name=$(basename $0)
if [ $script_name = 'add' ]
then
    add_value=$[$1 + $2]
    echo the add value is $add_value
elif [ $script_name = 'multi' ]
then
    multi_value=$[$1 * $2]
    echo the multi valus is $multi_value
fi